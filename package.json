{
  "name": "3-lakes-logistics",
  "version": "1.0.0",
  "description": "3 Lakes Logistics mobile app",
  "main": "index.js",
  "scripts": {
    "dev": "capacitor serve",
    "build": "mkdir -p public public/shared public/driver-pwa && cp 'index (8).html' public/index.html && cp eagleeye.html public/eagleeye.html && cp privacy-policy.html public/ && cp data-safety.html public/ && cp terms-of-service.html public/ && cp carrier-agreement.html public/ && cp broker-agreement.html public/ && cp sign.html public/ && cp robots.txt public/ && cp sitemap.xml public/ 2>/dev/null; cp shared/config.js public/shared/config.js && cp -r driver-pwa/* public/driver-pwa/ && node scripts/inject-config.js; exit 0",
    "cap:add:ios": "capacitor add ios",
    "cap:add:android": "capacitor add android",
    "cap:sync": "capacitor sync",
    "cap:open:ios": "capacitor open ios",
    "cap:open:android": "capacitor open android",
    "cap:build:ios": "capacitor build ios",
    "cap:build:android": "capacitor build android"
  },
  "keywords": ["3lakes", "logistics", "driver", "app"],
  "author": "3 Lakes Logistics",
  "license": "MIT",
  "devDependencies": {
    "@capacitor/cli": "^8.3.4",
    "@capacitor/core": "^8.3.4"
  },
  "dependencies": {
    "@capacitor/ios": "^8.3.4",
    "@capacitor/android": "^8.3.4"
  }
}
