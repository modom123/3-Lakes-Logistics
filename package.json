{
  "name": "3-lakes-logistics",
  "version": "1.0.0",
  "description": "3 Lakes Logistics mobile app",
  "main": "index.js",
  "scripts": {
    "dev": "capacitor serve",
    "build": "mkdir -p public public/shared public/driver-pwa && cp eagleeye.html public/index.html && cp eagleeye.html public/eagleeye.html && cp carrier-agreement.html public/ && cp broker-agreement.html public/ && cp sign.html public/ && cp privacy-policy.html public/ && cp terms-of-service.html public/ && cp light-fleet.html public/ && cp website.html public/website.html 2>/dev/null; cp shared/config.js public/shared/config.js && cp -r driver-pwa/* public/driver-pwa/ && node scripts/inject-config.js; exit 0",
    "build:mobile": "bash scripts/build-mobile.sh",
    "cap:add:android": "capacitor add android",
    "cap:add:ios": "capacitor add ios",
    "cap:sync": "capacitor sync",
    "cap:sync:android": "npm run build:mobile && capacitor sync android",
    "cap:open:android": "capacitor open android",
    "cap:open:ios": "capacitor open ios",
    "cap:build:ios": "capacitor build ios",
    "cap:build:android": "capacitor build android",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:report": "playwright show-report"
  },
  "keywords": [
    "3lakes",
    "logistics",
    "driver",
    "app"
  ],
  "author": "3 Lakes Logistics",
  "license": "MIT",
  "devDependencies": {
    "@capacitor/cli": "^8.3.4",
    "@capacitor/core": "^8.3.4",
    "@playwright/test": "^1.60.0",
    "playwright": "^1.60.0"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.4",
    "@capacitor/ios": "^8.3.4",
    "libsodium-wrappers": "^0.8.4"
  }
}
